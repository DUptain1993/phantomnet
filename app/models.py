"""
All SQLAlchemy model classes were moved here unchanged from the
original `phantom_c2_server.py` (lines 35-196 & 265-181 etc.).

Copy each class exactly as-is so behaviour stays identical.
"""
# ... paste full model definitions here ...
